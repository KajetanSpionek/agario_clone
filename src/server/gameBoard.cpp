#include "gameBoard.hpp"

void GameBoard::getInstance(){

}

GameBoard::GameBoard(){
    //initialize mapSize
    //createInitialFood()
    //initialize server
    //server->start()
    //server->listen()
    
}

void GameBoard::run(){
    //main game loop

/*
    while(1){

        if(newPlayer connected ){
            new Player
            add new 3 foodItems
            void addNewFood
            push_back to collection
            
        } 

        if(playerMovement){
            collision detection
            check if within board boundaries

            update all players
        }
    }

*/
}

void GameBoard::createInitialFood(){
    //initialize 100 foodItem
    //void addNewFood
    //push_back to collection
}

void GameBoard::detectCollision(){
    //detect collision of two elements
    //check which element should be destroyed
    //if detected -  delete element (and Player)

}

void addNewFood(){
    // randomly pick new position
    // check if position is unoccupied
    //if true create new item
}
